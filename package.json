{
  "devDependencies": {
    "git-cz": "^4.9.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4"
  },
  "scripts": {
    "prepare": "git config core.hooksPath .husky && chmod -R +x .husky || true"
  }
}